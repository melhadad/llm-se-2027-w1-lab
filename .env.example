# Copy to .env and fill in ONE model provider plus a GitHub token.
# .env is gitignored. It must stay that way.
#
# A pre-commit hook scans staged changes for key-shaped strings. If you find
# yourself arguing with it, the hook is right and you are about to publish a
# credential.

# --- Model provider: pick ONE, set LLM_MODEL to match ---------------------
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GEMINI_API_KEY=AIzaxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
XAI_API_KEY=xai-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# DSPy/LiteLLM model string. Must match the key you set above.
#   anthropic/claude-sonnet-4-6
#   openai/gpt-5.1-mini
#   gemini/gemini-2.5-flash
#   xai/grok-4
LLM_MODEL=anthropic/claude-sonnet-4-6

# --- GitHub -------------------------------------------------------------
# Needed by tools/fetch_issues.py. Public repos only, so a token with NO
# scopes ticked is sufficient and is what you should create.
# Without it you get 60 requests/hour; with it, 5000.
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
